export interface PlaceT {
  id: string;
  name: string;
  address: string;
  endTime: Date;
  latitude: number;
  longitude: number;
  maxPlayers: number;
  maxPrice: number;
  minPlayers: number;
  minPrice: number;
  openTime: Date;
  phoneNumber: string;
  site: string;
}
